---
from: mac
to: wsl
sent_at: 2026-04-28T07:06:00+09:00
status: open
---

📸 /insta-post 핸드오프 (4/26·4/27 2건 연속, Mac 토큰 사망)

목표:
2026-04-26, 2026-04-27 두 날짜 worklog 를 인스타 자동 포스팅한다. Mac 쪽에서 시도했으나 토큰이 OAuthException code 200 으로 거부되어 막혔다. WSL 토큰 가설로 핸드오프.

목표 흐름:
강대종님이 4/27 22시경 "어제 토큰없어서 인스타 실패" 라며 SCP 로 WSL → Mac 토큰 회수를 요청. SCP 자체는 성공해서 Mac 의 ~/.claude/secrets/instagram.json mtime 이 04-28 07:00 으로 갱신됐는데, 그 토큰으로 호출하니 /media, /debug_token, /me 전부 같은 OAuthException code 200. "Cannot call API for app 935709765996432 on behalf of user 122168701790723581". 60일 만료(2026-04-24 발급) 가 아니라 앱-유저 권한 자체가 끊긴 상태로 보임. WSL 의 토큰이 실제로 살아있는지(=Mac 으로 SCP 한 사본과 다른지) 검증부터 해줘.

할일:
1. `git pull` 후 이 핸드오프 파일 읽음
2. WSL 의 ~/.claude/secrets/instagram.json 으로 /me 호출 테스트:
   `TOKEN=$(python -c "import json; print(json.load(open('/home/.../...claude/secrets/instagram.json'))['access_token'])")` 후 `curl -s "https://graph.facebook.com/v21.0/me?access_token=$TOKEN"`
3. (a) WSL 토큰이 살아있으면 (정상 응답) → `/insta-post 2026-04-26` 실행, 성공 후 `/insta-post 2026-04-27`. permalink 2개 텔레그램 reply
4. (b) WSL 토큰도 같은 OAuthException code 200 이면 → 즉시 중단. 텔레그램 reply 1통으로 "WSL 토큰도 죽음, 앱-유저 권한 자체 깨진 상태, Meta 재발급 필요" 보고. 강대종님이 수동 업로드 폴백 vs 재발급 결정할 수 있게 PNG 2장(`render.py --worklog --date <date>` 만 돌린 결과) 텔레그램 첨부도 같이 송신
5. 어느 결과든 ~/claude-skills/handoffs/2026-04-28-0706-wsl-mac-insta-post-426-427-reply.md 작성·push (frontmatter status=done, reply_to 본 파일)

금지사항:
- WSL 도 토큰 죽었으면 거기서 멈춤. Meta Developer 콘솔 자동 조작·재발급 시도 금지 (강대종님이 직접 해야 함)
- `~/.claude/secrets/instagram.json` 내용 텔레그램·로그·git 어디에도 노출 금지 (token len, mtime, ig_user_id 까지만 OK)
- 재호출 force 금지 — posted.json 에 4/26 또는 4/27 이미 있으면 정상 종료 (Mac 호출은 실패했으니 양쪽 다 비어있을 가능성 높음)

종료 조건:
- 성공 경로: permalink 2개 텔레그램 reply + handoffs/ reply 파일 status=done push
- 토큰 사망 경로: 텔레그램 1통 보고 + PNG 2장 첨부 + handoffs/ reply 파일 status=done push (이번 사이클은 실패로 종결)

참고:
- Mac 쪽 검증 결과: /me, /debug_token, /media 모두 OAuthException code 200 (fbtrace_id 패턴 동일). app id 935709765996432, user id 122168701790723581
- 4/26 PNG 는 Mac /Users/user/insta-autopost/out/card-20260428-070118.png 에 이미 렌더돼있음 (참고용, WSL 에서는 자체 render 다시)
- 메모리 project_instagram_autopost_success.md 에 발급 루트(use case "기타/비즈니스" + FB 페이지 + 크리에이터) 기록되어 있음 — 강대종님이 재발급 결정 시 참고
