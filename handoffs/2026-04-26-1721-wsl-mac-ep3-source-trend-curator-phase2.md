---
from: wsl
to: mac
sent_at: 2026-04-26T17:21:00+09:00
status: info
purpose: Ep3 초안에 합칠 수 있는 WSL 시야의 trend-curator Phase 2 작업 디테일. 액션 아님.
---

🪟 [Ep3 보충자료 — 2026-04-26 오후 trend-curator Phase 2 라우팅 작업 (WSL 시야)]

맥본진은 새벽~오전 4-노드 셋업 의사결정(라파→맥미니 흐름, M1 발견)을 가지고 있고, 이쪽 WSL은 16:00~17:00 KST 1시간 동안 Phase 2(LLM 실호출) 코드 작업·정정 흐름을 가지고 있어요. Ep3 에 합치면 같은 인프라 위에 "결정 → 구현 → 검증 → 막힘" 한 사이클이 완성됩니다.

## 시간순 모먼트 (글감)

### 15:52 — "끝냈어" 의 검증
강대종님 첫 메시지: *"이전 세션에서 데스크탑 노트북 올레마 llm 연동 클로드코드 소넷 연동 끝냈어"*

→ 듣고 그냥 메모리에 박을 뻔. 검증해보니 "소넷 연동" 은 **아직 안 됨**. 실제로 된 건 trend-curator routing 1단계 (SSH 트리거)뿐. LLM 실호출은 아직 미연결.

**글감 톤**: "사용자 말을 그대로 메모리에 박는 게 위험한 이유 — 시점·정의·진척도 셋이 다 어긋날 수 있다."

### 16:01 — qwen2.5 의 정체
WSL본진 ollama 찔러봤더니 `qwen2.5:7b-instruct` + `nomic-embed-text` 가 깔려있음. 메모리는 "llama3.1:8b" 라 적혀있어서 처음엔 메모리 stale 인 줄 알았음.

강대종님: *"qwen2.5는 설치안했어"* → 잠시 후 *"아 기억이 채팅봇 만들 때 깔았었어 기억은나"*

→ 한 달 전 기억이 챗봇 프로젝트 잔여물이 오늘 4-노드 점검 중에 발견됨. 강대종님 본인도 "ㅋㅋ 모르는 사이에 깔린 거네요" 반응.

**글감 톤**: "AI 인프라는 누적된다. 한 달 전에 깐 모델이 오늘 다른 작업 중에 부산물로 발견되는 흔한 모먼트."

### 16:11 — 추측 vs 실측의 갭
처음 추측: 노트북 6GB GPU 니까 setup-local-llm.ps1 의 fallback 분기로 qwen2.5 깔렸을 것 (스크립트는 VRAM ≥ 7.5GB 이면 llama3.1, 미만이면 qwen2.5).

실측 (handoff 기록 + 직접 curl): 노트북도 **llama3.1:8b**. 4.92GB 모델이 6GB VRAM 에 풀로드 됨 (빡빡하지만 OK). 콜드스타트 7.83s, 워밍 19.6 tok/s.

→ "VRAM 빡빡하면 fallback 이 합리적" 은 일반론적으론 맞지만, 강대종님은 의도적으로 llama3.1 로 통일 (모델 검증 일관성). setup 스크립트의 자동분기는 결정에 영향 못 줌.

**글감 톤**: "스크립트의 디폴트 동작과 실제 운영 결정은 다르다."

### 16:16 — 토폴로지 정정
처음에 trend-curator 스크립트 읽고 "Mac → 노트북 1-hop SSH" 로 이해. 강대종님 정정: *"맥 → 본진, 그리고 본진에서 데스크탑 노트북 이렇게야"*.

코드 헤더 다시 읽어보니 명시되어 있었음: `# Hops Mac -> WSL master -> notebook (so SSH key footprint stays minimal).`

→ 이유: SSH 키 풋프린트 최소화. 본진 키만 두 노드에 등록. 맥은 본진까지만.

**글감 톤**: "SSH 키 관리 최소화 — 허브 토폴로지가 별 거 아닌 것 같지만 여러 외곽 노드가 늘어날수록 가치 큼."

### 16:22 — "트렌드큐레이터는 데스크탑 아니었어?"
강대종님 *"트렌드큐레이터는 노트북이 아니라 데스크탑에 돌리는거 아니었어?"*

코드 확인: `NOTEBOOK_HOST="100.89.67.22"` (노트북). 데스크탑 변종 스크립트 없음.

분석:
- 트렌드큐레이터 매일 10:00 KST 실행 (낮, "밤 배치" 아님)
- 데스크탑 75 tok/s 가 노트북 22 tok/s 보다 3.4배 빠름
- 핸드오프 기록의 "노트북 1차 요약" 표현이 그대로 코드에 반영되었던 듯

→ 둘 중 골라야 함:
- **옵션 A** (트렌드큐레이터 자체를 데스크탑으로 옮김) = 데스크탑에 git clone, .env 안전 이전, launchd 수정 — 5단계
- **옵션 B** (실행은 노트북, LLM 호출만 데스크탑 ollama) = ollama URL 한 줄

### 16:27 — 비유로 풀어 설명
강대종님 *"b 내가 이해가 잘안가 쉽게 설명해줘"*

→ "기자 = 노트북, 통역사 = 데스크탑. 기자가 자기 자리에서 일하면서 필요할 때 통역사한테 전화 거는 것. 기자를 통역사 자리로 굳이 옮길 필요 없음." 이걸로 결정.

**글감 톤**: "엔지니어링 의사결정의 진짜 작업은 옵션 A·B 비교가 아니라, 비유로 옵션을 풀어서 사용자가 직관적으로 동의하게 만드는 것."

### 16:30 — 코드 (74줄 추가)
```python
OLLAMA_URL = os.environ.get("OLLAMA_URL", "http://100.70.173.66:11434")
OLLAMA_MODEL = os.environ.get("OLLAMA_MODEL", "llama3.1:8b-instruct-q4_K_M")

def ollama_summarize_ko(prompt, max_tokens=80, timeout=60):
    # POST /api/generate, num_predict=80, temperature=0.3, fail-soft
    ...

def summarize_picked(hn_picked, gh_picked):
    # 매칭 아이템마다 한국어 한 문장 요약 prompt + LLM 호출
    # 실패 시 "(요약 실패)" 표기, 다음 아이템 계속
    ...
```

환경변수로 다 오버라이드 가능, fail-soft, 기존 텔레그램 발송과 마크다운 리포트 양쪽에 요약 흐름 통합.

### 16:35 — WSL본진 검증 통과
2 items / 6.8s. 실제 출력 일부:
- `이 저장소는 코드베이스 전체에 대한 심도 있는 파일을 인용한 기술 부채 ауд이트를 생성하는 클로드 코드 스킬입니다.`
- `클라우드 코드(Claude Code), 코덱스(Codex), 항중력(Antigravity), 오픈코드(OpenCode)와 같은 에이전트에 대한 크로스-에이전트 세션 재개 스キ야를 제공합니다.`

문제 발견:
- `ауд이트` ← 키릴 문자 섞임
- `스キ야` ← 카나 문자 섞임
- `항중력` ← Antigravity 직역 실패

→ 8B 모델 토큰화 한계 직접 노출. 의미는 통하지만 거슬림. README 의 "알려진 한계" 절에 추가됨.

**글감 톤**: "로컬 LLM 의 진짜 한계는 '느려서' 가 아니라 '가끔 키릴 섞인 한국어 뱉어서' 다."

### 16:40 — 노트북 git pull HTTPS 인증 hang
GitHub 에 push 까지 됐는데 노트북이 못 받아옴. HTTPS origin + 인증 캐시 없음 → SSH 세션 hang. scp 로 직접 덮어쓰기 시도 → 시스템 차단 ("공유 인프라 무단 수정 아님").

→ 원격에서 자동화로 해결 불가. 강대종님 직접 손 쓰는 한 줄 필요. 자동화의 마지막 1m 가 가장 어렵다는 흔한 패턴.

**글감 톤**: "에이전트가 모든 걸 해주지는 않는다. 마지막 git pull 한 번이 사람의 손을 부른다."

### 16:53 — 라파→맥미니 결정
강대종님 *"진접 본가에 맥미니 m1 16g 가지러가는중 라즈베리파이 살까하다가 맥미니있으면 그거 써도된다네 mac 세션이"*

맥본진 Claude 의 분석 (스크린샷으로 강대종님이 공유): 16GB M1 = 즉시 LLM 노드 합류 (15~25 tok/s, 라파5 의 1~2 와 비교 불가) + launchd plist 그대로 이전 + macOS 환경 통일 → "0원 + 셋업 30분".

→ 70만원 인프라 사려다가 0원으로 해결한 1시간. (사실 맥미니가 아니라 그냥 "집에 굴러다니는 자원" 발견기)

**글감 톤**: "인프라 부족 모먼트의 정답은 종종 '추가 구매' 가 아니라 '재고 점검' 이다."

## Ep3 잠재적 제목 후보

- "70만원 인프라 사려다가 0원으로 해결한 한 시간"
- "기자와 통역사 — 외곽 워커에 일 시키는 가장 단순한 방법"
- "에이전트가 못 하는 마지막 1m"
- "한 달 전 깐 모델이 오늘 부산물로 발견되는 모먼트"

## 메모리 갱신 결과 (참고)

오늘 1시간 작업으로 메모리에 박힌 파일:
- `project_4node_local_llm_infra.md` — 토폴로지 도식 + 노트북 6GB 실측치 추가
- `project_trend_curator_sonnet_routing.md` (신규) — Phase 1/2 구분, 옵션 B 채택 이유
- `reference_wsl_qwen25_legacy.md` (신규) — 기억이 챗봇 잔여물 명시

## 코드 변경 (참고)

- `ssamssae/trend-curator` 933a195 — Phase 2 한국어 요약 (curator.py +74줄)
- `ssamssae/trend-curator` a7653e0 — README Phase 2 절 추가

## 노트북 배포 상태 (Ep3 완성도에 영향)

- WSL本진/GitHub 까지 커밋 완료 ✅
- 노트북 git pull = 강대종님 직접 손 한 번 필요 (인증 hang)
- 내일 10:00 KST 자동실행은 옛 코드(요약 없는 1단계) — 안 망가짐, 다만 Phase 2 미반영
- Ep3 발행 시점이 노트북 배포 전이면 글에 "마지막 한 줄은 인간이 친다" 모먼트 정직하게 포함하면 좋음
